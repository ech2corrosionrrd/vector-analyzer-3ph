# CHANGELOG - VectorAnalyzer 3Ph

Всі зміни та покращення внесені під час сесії оновлення проєкту.

## [1.0.0] — 2026-03-24

Фіксований стабільний стан для релізу та відновлення.

### Змінено
- **`package.json`**: версія **1.0.0** (узгоджено з git-тегом `v1.0.0`).
- **GitHub Actions**: job `build` (Node 20, `npm ci`, `npm run build`) + job `deploy` (`upload-pages-artifact` → `dist`, `deploy-pages@v4`, environment `github-pages`).
- **Залежності**: `lucide-react` ^1.0.1 (peer React 19), оновлений `package-lock.json` для успішного `npm ci` у CI.

---

## [2026-03-20] — Документація та інструкції

### Змінено
- **README.md**: стандартне `npm install`; таблиця команд (`dev`, `build`, `preview`, `lint`); пояснення двох PWA (десктоп і `/mobile/`); уточнено експорт PDF (один лист A4, `html2canvas` / `html-to-image`); короткий розділ про деплой на GitHub Pages (`main`, `dist/`, джерело **GitHub Actions**).
- **start.bat**: автоматичне встановлення залежностей через `npm install` (без `--legacy-peer-deps` за замовчуванням).
- **CHANGELOG**: виправлено застарілий рядок про «видалення html2canvas» у записі «Аналіз і чистка коду» (див. нижче).
- **Репозиторій**: у `.gitignore` додано `*.zip`; з дерева джерел прибрано невикористані файли з колишнього `src/assets/` (шаблон Vite тощо).

---

## [2026-03-20] — Аналіз і чистка коду

### Додано
- Перетворення лінійної напруги у фазну для розрахунків (`toPhaseVoltage`); підказка у формі при виборі лінійної напруги.
- Власний `public/favicon.svg` для вкладки браузера та PWA.

### Змінено / прибрано
- Зайві залежності та імпорти (за потреби, зокрема непотрібні пакети на кшталт `mathjs`); неактивну кнопку «Поділитися».
- Експорт PDF: у поточній версії для знімка звіту використовуються **`html2canvas`** (основний шлях) та **`html-to-image`** (резерв).
- `lang="uk"` у `index.html`; фільтр експорту PDF через перевірку `nodeType`.
- Замість стороннього `icons.svg` (спрайт іконок) — один файл `favicon.svg`.

---

## [2026-03-20] - Оновлення та Оптимізація

### Додано
- **Конфігурація PWA**: Додано `vite-plugin-pwa` у `vite.config.js` для підтримки офлайн-режиму та інсталяції на смартфони.
- **Діагностика**: Покращено логіку визначення чергування фаз та перевернутої полярності.
- **Автоматичне розгортання**: Створено GitHub Actions workflow (`.github/workflows/deploy.yml`) для автоматичної публікації на GitHub Pages.
- **Інструкції**: Додано `README.md` з актуальними кроками по встановленню.
- **Git**: Ініціалізовано локальний репозиторій та зроблено перший коміт.

### Змінено
- **Метадані**: Placeholder-назви "-----" замінено на професійну назву `VectorAnalyzer 3Ph`.
- **Експорт PDF**: Оновлено функцію експорту — кнопки керування тепер автоматично ховаються у згенерованому звіті.
- **Стилізація**: Покращено візуальну ієрархію та акценти на векторній діаграмі.

### Виправлено
- Виправлено структуру папок для коректної роботи GitHub Pages (вимоги до розміщення `.github`).
- Усунено проблеми з відображенням SVG-маркерів при експорті (фільтр у `toPng`).

---
Спроєктовано для інженерів-метрологів.
